# secrets & local config
photo_ocr_config.json
service_account*.json
oauth_client*.json
drive_token*.json
.env

# local data (never push rider PII)
data/
*.xlsx
*.jpg
*.jpeg
*.png
docs/poc/poc_results.json
test-images/
archive/

# python
__pycache__/
*.pyc

# node — frontend/dist IS committed (Render has no Node build step)
frontend/node_modules/

# scratch
*.backup*
.claude/

# local diagnostic dumps (rider names, amounts) — never belong in the repo
.diag_*.json

# Phase 2 sample albums + pairing outputs (rider photos, amounts)
Phase2/
