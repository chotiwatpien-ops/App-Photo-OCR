---
name: Rider Photo OCR
description: โต๊ะตรวจหลักฐาน — หน้าจอที่วางข้อมูลที่ AI อ่านได้ไว้ข้างรูปที่มันอ่านมา แล้วให้คนตัดสิน
colors:
  ink: "oklch(0.279 0.041 260.031)"
  ink-soft: "oklch(0.446 0.043 257.281)"
  ink-muted: "oklch(0.554 0.046 257.417)"
  ink-faint: "oklch(0.704 0.04 256.788)"
  surface: "#ffffff"
  surface-tint: "oklch(0.984 0.003 247.858)"
  ground: "oklch(0.968 0.007 247.896)"
  hairline: "oklch(0.929 0.013 255.508)"
  hairline-strong: "oklch(0.869 0.022 252.894)"
  chrome: "oklch(0.208 0.042 265.755)"
  decide: "oklch(0.505 0.213 27.518)"
  decide-ground: "oklch(0.936 0.032 17.717)"
  decide-ground-soft: "oklch(0.971 0.013 17.38)"
  incomplete: "oklch(0.555 0.163 48.998)"
  incomplete-ground: "oklch(0.962 0.059 95.617)"
  incomplete-ground-soft: "oklch(0.987 0.022 95.277)"
  settled: "oklch(0.508 0.118 165.612)"
  settled-ground: "oklch(0.95 0.052 163.051)"
  commit: "oklch(0.596 0.145 163.225)"
  wayfinding: "oklch(0.546 0.245 262.881)"
  wayfinding-ground: "oklch(0.97 0.014 254.604)"
  wayfinding-edge: "oklch(0.809 0.105 251.813)"
  waiting: "oklch(0.443 0.11 240.79)"
  waiting-ground: "oklch(0.977 0.013 236.62)"
typography:
  display:
    fontFamily: "Segoe UI, Leelawadee UI, Tahoma, sans-serif"
    fontSize: "24px"
    fontWeight: 600
    fontFeature: "tabular-nums"
  headline:
    fontFamily: "Segoe UI, Leelawadee UI, Tahoma, sans-serif"
    fontSize: "18px"
    fontWeight: 600
  title:
    fontFamily: "Segoe UI, Leelawadee UI, Tahoma, sans-serif"
    fontSize: "16px"
    fontWeight: 600
  body:
    fontFamily: "Segoe UI, Leelawadee UI, Tahoma, sans-serif"
    fontSize: "14px"
    fontWeight: 400
  label:
    fontFamily: "Segoe UI, Leelawadee UI, Tahoma, sans-serif"
    fontSize: "12px"
    fontWeight: 400
  micro:
    fontFamily: "Segoe UI, Leelawadee UI, Tahoma, sans-serif"
    fontSize: "11px"
    fontWeight: 400
rounded:
  xs: "4px"
  md: "8px"
  lg: "12px"
  pill: "9999px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "20px"
components:
  button-commit:
    backgroundColor: "{colors.commit}"
    textColor: "{colors.surface}"
    rounded: "{rounded.md}"
    padding: "8px 16px"
    height: "44px"
    typography: "{typography.body}"
  button-commit-disabled:
    backgroundColor: "{colors.hairline-strong}"
    textColor: "{colors.surface}"
    rounded: "{rounded.md}"
  button-quiet:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.wayfinding}"
    rounded: "{rounded.md}"
    padding: "0 12px"
    height: "44px"
  button-danger:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.decide}"
    rounded: "{rounded.md}"
    padding: "0 20px"
    height: "48px"
  chip-decide:
    backgroundColor: "{colors.decide-ground}"
    textColor: "{colors.decide}"
    rounded: "{rounded.pill}"
    padding: "2px 8px"
    typography: "{typography.label}"
  chip-incomplete:
    backgroundColor: "{colors.incomplete-ground}"
    textColor: "{colors.incomplete}"
    rounded: "{rounded.pill}"
    padding: "2px 8px"
    typography: "{typography.label}"
  chip-settled:
    backgroundColor: "{colors.settled-ground}"
    textColor: "{colors.settled}"
    rounded: "{rounded.pill}"
    padding: "2px 8px"
    typography: "{typography.label}"
  card:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.ink}"
    rounded: "{rounded.lg}"
    padding: "20px"
  banner-decide:
    backgroundColor: "{colors.decide-ground-soft}"
    textColor: "{colors.decide}"
    rounded: "{rounded.md}"
    padding: "8px 12px"
  input-figure:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.ink}"
    rounded: "{rounded.md}"
    padding: "6px 8px"
    typography: "{typography.body}"
  input-figure-asking:
    backgroundColor: "{colors.incomplete-ground-soft}"
    textColor: "{colors.ink}"
    rounded: "{rounded.md}"
  row-selected:
    backgroundColor: "{colors.wayfinding-ground}"
    textColor: "{colors.ink}"
  nav-item-active:
    backgroundColor: "rgba(255,255,255,0.15)"
    textColor: "{colors.surface}"
    rounded: "{rounded.md}"
    padding: "0 12px"
    height: "44px"
---

# Design System: Rider Photo OCR

## Overview

**Creative North Star: "โต๊ะตรวจหลักฐาน" (The Audit Desk)**

หน้าจอนี้ไม่ได้มีไว้ให้ชื่นชม มันมีไว้ให้คนตัดสินใจเรื่องเงินของคนอื่นได้เร็วและไม่พลาด ทุกแถวที่ขึ้นมาคือข้อกล่าวอ้างว่า "ไรเดอร์คนนี้วิ่งเที่ยวนี้ ได้เท่านี้" และหน้าที่ของหน้าจอคือ **วางหลักฐานไว้ข้างข้อกล่าวอ้างนั้น** — รูปสลิปจริง ตัวเลขที่ AI อ่านได้ และเหตุผลว่าทำไมระบบถึงไม่กล้าตัดสินเอง — แล้วถอยออกมาให้คนตัดสิน

ระบบจึงเงียบโดยตั้งใจ พื้นเทาอ่อน การ์ดขาว เส้นขอบบาง ไม่มีเงา ไม่มีการเคลื่อนไหว สีทั้งหมดถูกสงวนไว้บอกสถานะเท่านั้น เพราะบนหน้าจอที่มีตัวเลขเป็นร้อยตัว สิ่งเดียวที่ควรดึงตาคือ **แถวที่ต้องการคน** ความหนาแน่นสูงเป็นคุณสมบัติ ไม่ใช่ข้อเสีย — คนใช้ต้องการเห็นไรเดอร์ทั้งกลุ่มในจอเดียว ไม่ใช่การ์ดสวยๆ ที่ต้องเลื่อนสิบครั้ง

น้ำเสียงของข้อความคือเพื่อนร่วมงานที่รู้เรื่อง ไม่ใช่ระบบ — มันบอกตัวเลขที่ขัดกันพร้อมส่วนต่างเป็นบาท และบอกว่าต้องทำอะไรต่อ

**Key Characteristics:**
- หลักฐานอยู่ข้างข้อกล่าวอ้างเสมอ รูปกับตัวเลขอยู่ในสายตาเดียวกัน
- แบนราบ เส้นขอบ 1px คือระบบความลึกทั้งหมด
- สีมีความหมายตายตัว ไม่มีสีตกแต่ง
- หนาแน่นแต่อ่านออก ตัวเลขเรียงหลักตรงกันทุกคอลัมน์
- ภาษาไทยทั้งหมด และข้อความบอกตัวเลขของตัวเองเสมอ

## Colors

พาเลตต์คือเทาอมน้ำเงิน (slate) เป็นโครงทั้งหมด แล้วมีสีบอกสถานะสี่สีที่ปรากฏเฉพาะตอนมีอะไรต้องบอก

### Primary
- **เขียวลงมือ (commit)** `oklch(0.596 0.145 163.225)`: ปุ่มที่ลงมือจริงและย้อนไม่ได้ในทางปฏิบัติ — อนุมัติ · บันทึกลง Excel · ดาวน์โหลดไฟล์ส่งลูกค้า เป็นสีทึบสีเดียวในหน้าจอ
- **เขียวลงตัว (settled)** `oklch(0.508 0.118 165.612)` บนพื้น `oklch(0.95 0.052 163.051)`: ตัวเลขที่ตรวจแล้วตรง · จำนวนที่อนุมัติแล้ว · ป้าย "ครบ"

### Secondary
- **แดงต้องตัดสิน (decide)** `oklch(0.505 0.213 27.518)` บนพื้น `oklch(0.936 0.032 17.717)` และพื้นอ่อน `oklch(0.971 0.013 17.38)`: เลขขัดกัน · รูปซ้ำ · เคยอนุมัติไปแล้ว · จำนวนที่รอคน แดงในแอปนี้ไม่ได้แปลว่า "ผิดพลาด" แต่แปลว่า **ต้องมีคนตัดสิน**
- **เหลืองข้อมูลไม่พอ (incomplete)** `oklch(0.555 0.163 48.998)` บนพื้น `oklch(0.962 0.059 95.617)` และพื้นอ่อน `oklch(0.987 0.022 95.277)`: รูปไม่มีเลขพอให้ตรวจ · ยังไม่มีวันที่ · ช่องที่ยังต้องกรอก · รอบที่ถูกข้าม

### Tertiary
- **น้ำเงินทางเดิน (wayfinding)** `oklch(0.546 0.245 262.881)`: ทุกอย่างที่กดแล้วพาไปที่อื่นหรือเปลี่ยนสิ่งที่เห็น — ลิงก์ · แถวที่กำลังเลือก (พื้น `oklch(0.97 0.014 254.604)` ขอบ `oklch(0.809 0.105 251.813)`) · ช่องที่เพิ่งพิมพ์แก้
- **ฟ้ารอผล (waiting)** `oklch(0.443 0.11 240.79)` บนพื้น `oklch(0.977 0.013 236.62)`: งานที่ส่งออกไปแล้วและต้องรอ ไม่ต้องทำอะไร — batch ที่ AI กำลังอ่าน

### Neutral
- **หมึก (ink)** `oklch(0.279 0.041 260.031)`: ข้อความหลักทั้งหมด
- **หมึกรอง (ink-soft)** `oklch(0.446 0.043 257.281)` และ **หมึกจาง (ink-muted)** `oklch(0.554 0.046 257.417)`: คำอธิบาย หัวตาราง หน่วย
- **หมึกแผ่ว (ink-faint)** `oklch(0.704 0.04 256.788)`: ข้อมูลประกอบที่อ่านเมื่อต้องการเท่านั้น เช่น ชื่อไฟล์ เลข job
- **พื้นจม (ground)** `oklch(0.968 0.007 247.896)`: พื้นหลังของทั้งหน้า การ์ดขาวลอยอยู่บนนี้
- **เส้นผม (hairline)** `oklch(0.929 0.013 255.508)`: ขอบการ์ดและเส้นคั่นตาราง — นี่คือระบบความลึกทั้งหมดของแอป
- **แถบเครื่อง (chrome)** `oklch(0.208 0.042 265.755)`: แถบหัวเว็บและปุ่มสั่งรอบ เป็นที่เดียวที่ใช้พื้นเข้ม

### Named Rules

**The Colour-Is-State Rule.** สีไม่เคยถูกใช้เพื่อความสวย ทุกครั้งที่สีปรากฏ มันกำลังตอบคำถามว่า "แถวนี้ต้องการอะไรจากฉัน" — แดง = ต้องตัดสิน · เหลือง = ข้อมูลไม่พอ · เขียว = ผ่านแล้ว · ฟ้า = รอเครื่องอื่น · น้ำเงิน = กดแล้วไปต่อ หน้าจอที่สีทั้งหมดหายไปแปลว่าไม่มีอะไรค้าง และนั่นคือหน้าจอที่ถูกต้อง

**The Two-Layer Rule.** สีสถานะทุกสีมีสองชั้นเสมอ: พื้นอ่อนสำหรับกล่องข้อความยาว และพื้นเข้มขึ้นหนึ่งขั้นสำหรับป้ายสั้น ห้ามใช้สีทึบเป็นพื้นของข้อความ ยกเว้นปุ่มลงมือสีเขียวเท่านั้น

> ข้อสังเกต: กฎสองข้อนี้ผมสรุปจากสิ่งที่โค้ดทำอยู่แล้ว (2026-09-19) ไม่ใช่กฎที่ทีมเคยประกาศไว้ — เฟียสตอบว่า "ยังไม่ได้คิด แล้วแต่หน้า" จึงบันทึกของจริงไว้เป็นมาตรฐาน แก้ได้ถ้าจะเอาทางอื่น

## Typography

**Display / Body Font:** Segoe UI (สำรอง Leelawadee UI, Tahoma, sans-serif) — ฟอนต์ระบบ เลือกเพราะ Leelawadee UI เรนเดอร์ภาษาไทยได้ดีบน Windows ซึ่งเป็นเครื่องที่ทีมใช้ ไม่มีฟอนต์สำหรับโชว์แยกต่างหาก

**Character:** ไม่มีบุคลิก และตั้งใจให้เป็นอย่างนั้น ตัวอักษรมีหน้าที่หายไปจากความสนใจ เหลือไว้แต่ตัวเลขกับรูป น้ำหนักมีแค่สองระดับ (400 กับ 600) ขนาดต่างกันน้อย — ลำดับชั้นมาจากสีและระยะ ไม่ใช่จากขนาด

### Hierarchy
- **Display** (600, 24px, tabular-nums): ตัวเลข KPI ในหน้า Dashboard เท่านั้น
- **Headline** (600, 18px): ชื่อสัปดาห์บนหัวการ์ด
- **Title** (600, 16px): หัวข้อของแต่ละส่วน เช่น "ดูดรูปจาก Google Drive"
- **Body** (400, 14px): เนื้อหาหลักทั้งหมด รวมถึงทุกแถวในตาราง
- **Label** (400, 12px): หัวตาราง ป้ายสถานะ คำอธิบายใต้ช่องกรอก
- **Micro** (400, 10–11px): ข้อมูลที่อ่านเมื่อสงสัยเท่านั้น เช่น คำเตือนในป้าย ชื่อไฟล์ย่อ

### Named Rules

**The Tabular Rule.** ทุกตัวเลขที่อยู่ในคอลัมน์เดียวกันหรือถูกเทียบกันต้องใช้ `tabular-nums` เสมอ — ค่าเงิน จำนวนเที่ยว ระยะทาง ตัวนับคิว หลักที่ไม่ตรงกันทำให้คนอ่านผิดและเป็นเงินของคนอื่น

**The Number-Says-Why Rule.** ข้อความบอกสถานะต้องพกตัวเลขของตัวเองมาด้วย ไม่ใช่ป้ายลอยๆ — เขียนว่า "รายได้ 81 ≠ พื้นฐาน 77 + โบนัส 0 + เทอร์โบ 0 = 77 · ต่างกัน 4 บาท" ไม่ใช่ "ข้อมูลไม่ถูกต้อง"

## Layout

พื้นหน้าเป็นเทาอ่อน (`ground`) มีการ์ดขาวขอบบางวางเรียงลงมาในคอลัมน์เดียว ห่างกัน 16–24px กว้างสุด `max-w-screen-2xl` กึ่งกลาง ระยะขอบ 16px บนจอเล็ก 24px บนจอใหญ่

ข้างในการ์ดใช้ระยะ 20px รอบด้าน ตารางใช้ระยะแถว 6px บน-ล่างเพื่อให้เห็นไรเดอร์ได้มากที่สุดในจอเดียว

**เบรกพอยต์ที่ใช้จริง** — `sm` 640px · `md` 768px · `lg` 1024px (ไม่ใช้ `xl` ขึ้นไป)

- **ต่ำกว่า 640px** ตารางตัดคอลัมน์ที่ไว้ดูทีหลังออก · รูปสองครึ่งเรียงบนล่างแทนข้างกัน · ปุ่มเต็มความกว้าง
- **ต่ำกว่า 768px** แถบเมนูบนสุดเปลี่ยนเป็นแถวปุ่มเลื่อนแนวนอนใต้ชื่อแอป
- **ต่ำกว่า 1024px** คิวตรวจเปลี่ยนจากสองคอลัมน์เป็นหน้าเดียว รายการพับเก็บ และการกระทำย้ายไปอยู่แถบล่างที่ติดหน้าจอ

### Named Rules

**The 44 Rule.** ทุกสิ่งที่นิ้วต้องกดมีความสูงอย่างน้อย 44px และมีระยะห่างจากเพื่อนบ้านพอที่จะไม่กดพลาด ลิงก์ข้อความเล็กๆ ที่เคยพอสำหรับเมาส์ ไม่พอสำหรับหน้าจอนี้อีกต่อไป — หน้าคิวตรวจถูกใช้บนมือถือจริง

**The No-Sideways Rule.** หน้าเว็บต้องไม่เลื่อนแนวนอนที่ความกว้าง 320px ขึ้นไป ตารางที่กว้างเกินต้องตัดคอลัมน์หรือใส่กล่องเลื่อนของตัวเอง ไม่ใช่ปล่อยให้ทั้งหน้าเลื่อน

## Elevation & Depth

**แอปนี้แบน** ความลึกทั้งหมดมาจากสามอย่าง: การ์ดขาวบนพื้นเทา · เส้นขอบ 1px สี `hairline` · และการเว้นระยะ ไม่มีเงาในระบบ

ในโค้ดปัจจุบันเหลือ `shadow-sm` อยู่ 6 จุดและ `shadow` 1 จุด ซึ่งเป็นของค้างจากตอนแรก ไม่ใช่ระบบ — เทียบกับเส้นขอบที่ใช้อยู่ 47 จุด งานใหม่ไม่ต้องใส่เงา และเมื่อแก้ไฟล์เดิมที่มีเงาอยู่ ให้ถอดออกได้เลย

มีที่เดียวที่ใช้ความลึกจริงคือ **กล่องขยายรูป** ซึ่งใช้พื้นดำโปร่ง 80% คลุมทั้งจอ เพราะตอนนั้นรูปคือทุกอย่าง และ **แถบล่างบนมือถือ** ที่ใช้ขอบบน 1px กับพื้นขาวโปร่ง 95% + blur เพื่อให้เนื้อหาที่เลื่อนอยู่ข้างใต้ยังพอเห็น

### Named Rules

**The Hairline Rule.** ถ้าต้องแยกสองสิ่งออกจากกัน ใช้เส้น 1px สี `hairline` ก่อนเสมอ ถ้าเส้นไม่พอให้ใช้ระยะห่าง ถ้าระยะห่างไม่พอค่อยเปลี่ยนพื้นเป็น `surface-tint` — เงาไม่อยู่ในลำดับนี้

> ข้อสังเกต: เฟียสตอบว่า "ไม่เคยคิด แล้วแต่นาย" ผมจึงยึดตามหลักฐานในโค้ด (ขอบ 47 : เงา 7) แล้วประกาศให้แบนเป็นมาตรฐาน

## Shapes

มุมโค้งมีสี่ระดับและแต่ละระดับมีหน้าที่ของมัน: **12px** สำหรับการ์ดที่เป็นภาชนะของเนื้อหา · **8px** สำหรับทุกอย่างที่กดได้และช่องกรอก (ตัวที่ใช้บ่อยที่สุด) · **4px** สำหรับรูปย่อและป้าย `kbd` · **วงรีเต็ม** สำหรับป้ายสถานะเท่านั้น

รูปทรงป้ายสถานะเป็นแคปซูลเสมอ เพื่อให้แยกจากปุ่มด้วยสายตาโดยไม่ต้องอ่าน — แคปซูล = สถานะ (อ่านอย่างเดียว) · สี่เหลี่ยมมุมมน 8px = กดได้

ไม่มีการตัดขอบแบบอื่น ไม่มีมุมเอียง ไม่มีเส้นขอบหนากว่า 1px ยกเว้นขอบบนของแถบล่างบนมือถือ

## Components

### Buttons
- **Shape:** มุมโค้ง 8px ทุกตัว
- **ลงมือ (commit):** พื้นเขียวทึบ ตัวหนังสือขาว สูง 44px ระยะใน 8×16px — ใช้กับการกระทำที่เกิดผลจริงเท่านั้น หนึ่งหน้าจอควรมีไม่เกินหนึ่งถึงสองปุ่ม
- **Disabled:** พื้นเทา `hairline-strong` ตัวหนังสือขาว ไม่ลดความทึบ เพื่อให้ยังอ่านออกว่าปุ่มนี้มีอยู่
- **เงียบ (quiet):** พื้นขาว ขอบ `hairline-strong` ตัวหนังสือน้ำเงิน — รีเฟรช ก่อนหน้า ถัดไป
- **อันตราย (danger):** พื้นขาว ขอบแดงอ่อน ตัวหนังสือ `decide` **แดงตั้งแต่ยังไม่แตะ** ไม่รอ hover เพราะนิ้วไม่มี hover
- **Hover:** เข้มขึ้นหนึ่งขั้น (เขียว 600→700) หรือขอบเปลี่ยนสี ไม่มีการขยับตำแหน่ง

### Chips
- **Style:** แคปซูล ระยะใน 2×8px ขนาด 12px พื้นอ่อนคู่กับหมึกเข้มของสีเดียวกัน
- **State:** ใช้บอกสถานะอย่างเดียว ไม่เคยกดได้ · ห้ามขึ้นบรรทัดใหม่ (`whitespace-nowrap`) เพราะป้ายที่แตกสองบรรทัดทำให้แถวในตารางสูงไม่เท่ากันและอ่านยาก

### Cards
- **Corner:** 12px · **Background:** ขาว · **Border:** 1px `hairline` · **Padding:** 20px · **Shadow:** ไม่มี
- การ์ดไม่ซ้อนการ์ด ถ้าต้องแบ่งส่วนข้างในให้ใช้เส้นคั่นกับหัวข้อ

### Inputs
- **Style:** ขอบ 1px `hairline` มุม 8px พื้นขาว ตัวเลขใช้ `tabular-nums`
- **Focus:** ขอบเปลี่ยนเป็นน้ำเงินพร้อมวงแหวน 2px สีน้ำเงินอ่อน
- **กำลังพิมพ์แก้ (dirty):** ขอบและพื้นเป็นน้ำเงินอ่อน บอกว่ายังไม่ถูกบันทึก
- **ยังต้องกรอก (asking):** ขอบและพื้นเป็นเหลือง (หรือแดงถ้าขาดแล้วอนุมัติไม่ได้) พร้อมป้ายข้างชื่อช่องว่า "ยังขาด" / "ต้องกรอกก่อนอนุมัติ" — และช่องจะเลิกร้องขอทันทีที่เริ่มพิมพ์

### Navigation
- แถบหัวเว็บพื้น `chrome` ปุ่มเมนูมุม 8px สูง 44px · ปุ่มที่กำลังอยู่ใช้พื้นขาวโปร่ง 15% · ต่ำกว่า 768px แถบเมนูย้ายลงมาเป็นแถวเลื่อนแนวนอนใต้ชื่อแอป ไม่ใช้เมนูแฮมเบอร์เกอร์ เพราะสี่เมนูกดตรงๆ เร็วกว่าเปิดสองจังหวะ

### แถวคิว (signature)
แถวในคิวตรวจคือองค์ประกอบที่นิยามแอปนี้ เรียงจากซ้าย: **รูปย่อ 40px** → **ป้ายเหตุผล** → **ชื่อไฟล์จาง** → **ยอดเงิน** ป้ายเหตุผลมาก่อนชื่อไฟล์เสมอ เพราะสิ่งที่คนต้องรู้ก่อนคือ "แถวนี้ต้องการอะไร" ไม่ใช่ "แถวนี้ชื่ออะไร" แถวที่กำลังดูใช้พื้นน้ำเงินอ่อนกับวงแหวน 1px ด้านใน

### กล่องเหตุผล (signature)
กล่องเต็มความกว้างเหนือรูป มีสองบรรทัด: **บรรทัดแรกคือข้อเท็จจริงพร้อมตัวเลข** บรรทัดที่สองคือสิ่งที่ต้องทำต่อ สีของกล่องมาจากสถานะของแถว กล่องนี้คือเหตุผลที่คนไม่ต้องเดาว่าทำไมระบบถึงหยุด

## Do's and Don'ts

### Do:
- **Do** วางรูปหลักฐานกับตัวเลขที่อ่านได้ไว้ในสายตาเดียวกันเสมอ ถ้าหน้าจอขอให้ตัดสินใจ มันต้องแสดงสิ่งที่ใช้ตัดสิน
- **Do** ใช้ `tabular-nums` กับตัวเลขทุกตัวที่ถูกเทียบกัน
- **Do** ให้ข้อความสถานะพกตัวเลขของตัวเองมาด้วย พร้อมบอกวิธีแก้
- **Do** ทำให้ทุกเป้าที่นิ้วกดสูงอย่างน้อย 44px
- **Do** ใช้เส้น 1px `hairline` เป็นตัวแบ่งอันดับแรก
- **Do** ตัดคอลัมน์ที่ไว้ดูทีหลังออกบนจอแคบ และให้มันกลับมาที่ 640px

### Don't:
- **Don't** ใช้สีเพื่อความสวย ทุกสีในแอปนี้เป็นคำตอบของคำถามว่าแถวนี้ต้องการอะไร
- **Don't** ใส่ความหมายไว้ใน `hover:` อย่างเดียว — ปุ่มลบเคยเป็นสีแดงเฉพาะตอน hover และบนมือถือมันกลายเป็นปุ่มธรรมดา
- **Don't** ใช้แถบสีหนาด้านซ้ายของการ์ดหรือแถวเพื่อบอกว่าถูกเลือก ใช้พื้นอ่อนกับวงแหวน 1px แทน
- **Don't** ใส่เงาให้ของใหม่ ระบบนี้แบน
- **Don't** ซ้อนการ์ดในการ์ด
- **Don't** ปล่อยให้ป้ายสถานะขึ้นบรรทัดใหม่ในตาราง
- **Don't** ซ่อนเมนูหรือการกระทำหลักบนจอเล็กโดยไม่มีอะไรมาแทน
